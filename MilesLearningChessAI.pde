//class MilesLearningChessAI extends AIChessPlayer { // implements? should AIChessPlayer be an interface?
  
  // create a graph such that each vertex is a board state and each edge is a piece move from one board state to another and some additional win/loss information
  // keep track of moves through a game
  // when the game is over, add one win or one loss to each edge traversed
  // have a learning mode where moves are mostly random/ new boards are explored
  // have a competitive mode where moves are made by looking at win probabilities of each edge
  
//}
